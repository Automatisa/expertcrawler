package org.ipen.miner.nlp.token;

import java.util.Set;

public interface StringSet extends Set<CharSequence>, CharSequence, Comparable<StringSet> {
	//int count();
	//Chain toChain(Tokenizer tokenizer);
}
